import React from 'react';
import Input from '../../../../common/_custom/Input2/Input';

function Right() {
    return (
        <div>
            <Input label='Search By Phone' value={''} onChange={() => {}} />
        </div>
    );
}

export default Right;
